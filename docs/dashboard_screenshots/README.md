# Dashboard Screenshots

Add screenshots here after running the dashboard locally:

```
make generate-data
make pipeline-local
make dashboard
```

Then screenshot each page (Product KPIs, Retention, Funnel Analysis, Feature Usage, Revenue, Anomaly Detection) and reference them in the main README, e.g.:

```markdown
![Product KPIs](docs/dashboard_screenshots/product_kpis.png)
```
